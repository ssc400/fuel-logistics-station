# 油站运距核对 Skill — 交付说明（供其他 agent 使用）

> 本说明用于把「运距核对」能力交接给另一个 agent：它该在何时用、怎么调、注意什么。

## 1. Skill 本体位置

```
C:/Users/Administrator/.workbuddy/skills/油站运距核对/
├── SKILL.md                      # 完整说明（本文件的权威来源）
└── scripts/
    ├── check_distance.py         # 主脚本：批量核对运距
    └── probe_source.py           # 辅助：体检源表（列识别/坐标异常扫描）
```

移交时把整个 `油站运距核对/` 目录打包给对方 agent 的 `~/.workbuddy/skills/` 即可，SKILL.md 会被自动加载。

## 2. 何时调用

用户提到以下任一关键词时启用：运距核对 / 运距复核 / 核对运距 / 油库到加油站距离 / 运费结算里程 / 高德里程 / 最短运距 / 高速优先运距。

## 3. 核心原则（血泪教训，必须遵守）

1. **核对"全部指定组合"，不能只核对原表有填值的。** 原表空白的线路也要算出高德里程并标"原表未填"——这些恰恰最该回填。
2. **油库列靠"与坐标底库匹配"识别，不能按列号范围扫。** 否则会把销量/经纬度/地址等列当运距列，造成大面积假异常。
3. **先 `--dry-run` 估算配额再实跑。** 免费 KEY 每日 2000 次；matrix 1 次算 ≤100 对，shortest/highway 每对 1 次。
4. **输出 xlsx 被 Excel/预览锁定时会 PermissionError**，脚本已自动改名另存；仍建议输出到未被打开的新文件名。

## 4. 三种核对方式

| 参数 | 名称 | API | 配额 | 说明 |
|---|---|---|---|---|
| `matrix`（默认） | 推荐最短 | `v3/distance` | 1 次算 ≤100 对 | 高德默认推荐路线≈最短；**全量普查用这个** |
| `shortest` | 最短里程 | `v3/direction/driving` strategy=2 | 每对 1 次 | 严格距离优先，附过路费 |
| `highway` | 高速优先 | `v5/direction/driving` strategy=34 | 每对 1 次 | 走高速，附过路费 |

> 实测广西路网三种结果基本一致（高德默认推荐就是高速）。真正拉开差距的是"不走高速"。日常核对用 `matrix` 即可。

## 5. 数据格式

**源表（加油站运距表）**：一站一行，油库运距横向铺成矩阵。脚本**自动识别列**，不依赖固定列号：

| 列 | 识别方式 |
|---|---|
| 加油站名 | 表头含"加油站/站名/客户名称/网点"，兜底第 3 列 |
| 地区 | 表头含"地区/区域/分公司/片区"，兜底第 2 列 |
| 站经纬度 | 表头含"经纬度/坐标"，格式 `108.596461,21.759848` |
| 各油库运距列 | 表头名能在油库坐标表匹配上（并经黑名单过滤） |

**油库坐标表**：A 列油库名 + 一列经纬度（表头含"经纬度/坐标"）。

## 6. 调用示例

```bash
PY="C:/Users/Administrator/.workbuddy/binaries/python/versions/3.13.12.old.17048/python.exe"
S="C:/Users/Administrator/.workbuddy/skills/油站运距核对/scripts/check_distance.py"

# 1) 先估配额（务必先跑）
"$PY" "$S" --plan plan.json --strategy matrix --dry-run

# 2) 实跑
"$PY" "$S" --plan plan.json --strategy matrix --out "F:/工作/运距核对结果.xlsx"
```

`plan.json` 示例（地区关键词做包含匹配，`"*"` 表示全部地区）：

```json
{
  "src": "F:/工作/加油站运距_已完善.xlsx",
  "depot_file": "F:/工作/油库位置_坐标_合并确认版.xlsx",
  "key": "高德WebService key",
  "regions": {
    "贺州": ["广西柳州油库", "广西桂林油库", "桂林羊角山石化油库"],
    "桂林": ["广西桂林油库", "桂林羊角山石化油库"]
  }
}
```

命令行方式（省略 `--depots` = 全部油库列；省略 `--regions` = 全部地区）：

```bash
"$PY" "$S" --src 源表.xlsx --depot-file 油库坐标.xlsx \
   --regions 贺州,桂林 --depots 广西桂林油库 \
   --strategy highway --out 结果.xlsx --key <key>
```

其他参数：`--cache` 缓存（默认 `scripts/_dist_cache.json`，按策略分区，重跑不重复消耗）、`--daily-cap` 每日配额上限（默认 1900）、`--dry-run` 只估算不调用。

## 7. 判定规则（双阈值，避免短途小偏差误报）

- `异常`：绝对偏差 >15km **且** 相对偏差 >20%
- `待确认`：绝对偏差 >8km **且** 相对偏差 >12%
- `正常`：其余有数据的
- `原表未填`：原表空白但已算出高德里程（重点关注，可回填源表）
- `无数据`：站或油库缺坐标，无法计算

## 8. 常见问题排查

| 现象 | 原因 | 处理 |
|---|---|---|
| 某站里程离谱（原表22km、高德558km） | 源表该站经纬度填错 | 用 `probe_source.py` 扫描离本区中心>150km 的站，让用户修正 |
| 某油库全部组合"无数据" | 油库坐标表缺坐标或名称对不上 | 补坐标；`match_depot` 三级匹配（原名→归一→子串）通常能兜住 |
| `CUQPS_HAS_EXCEEDED_THE_LIMIT` | 请求过快 | 已内置 0.55s 间隔，勿调小 |
| `USER_DAILY_QUERY_OVER_LIMIT` | 当日配额耗尽 | 脚本保存已完成部分，次日重跑自动续 |
| 输出 `PermissionError` | 目标 xlsx 正被预览/打开 | 脚本自动改名另存；或先关闭预览 |
| `v5/truck/route` 404 | 货车路径端点下线 | 不要用货车 API，用小车里程 |

## 9. 关联

- 工作站在线版运距复核页 `dist_verification.html`（浏览器端同款逻辑，支持地区/油库勾选 + 三种核对方式 + 分类筛选），部署于成品油物流工作空间站。
- 本 Skill 的在线版已集成进公网站点（见仓库 `dist_verification_v1996.html`），填 KEY 即可用。
